---

---

<nav
	class="fixed top-0 z-50 w-full border-b border-gray-200 bg-white/80 backdrop-blur-md transition-colors duration-300 dark:border-gray-700 dark:bg-slate-900/80"
>
	<div class="mx-auto w-full max-w-[85rem] px-4 sm:px-6 lg:px-8">
		<div class="flex h-16 items-center justify-between">
			<!-- Logo / Icon -->
			<a
				href="#home"
				class="group nav-link flex flex-shrink-0 cursor-pointer items-center gap-2"
				data-target="home"
			>
				<div
					class="flex h-8 w-8 items-center justify-center rounded-lg bg-blue-600 font-black tracking-tighter text-white transition-transform group-hover:rotate-3"
				>
					TR
				</div>
				<span class="text-lg font-bold text-slate-800 dark:text-white"
					>Tony Rosen</span
				>
			</a>

			<!-- Desktop Links -->
			<div class="hidden items-center gap-8 md:flex">
				<!-- Portfolio Link -->
				<a
					href="#portfolio"
					class="nav-link text-sm font-medium text-slate-600 transition-colors hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400"
					data-target="portfolio">Portfolio</a
				>

				<!-- Resume Link triggers View Change -->
				<a
					href="#resume"
					class="nav-link text-sm font-medium text-slate-600 transition-colors hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400"
					data-target="resume">Resume</a
				>

				<a
					href="#contact"
					class="text-sm font-medium text-slate-600 transition-colors hover:text-blue-600 dark:text-slate-300 dark:hover:text-blue-400"
					>Contact</a
				>

				<!-- Theme Switcher -->
				<button
					id="theme-toggle"
					type="button"
					aria-label="Toggle dark mode"
					class="rounded-full p-2 text-slate-600 transition-colors hover:bg-gray-100 focus:ring-2 focus:ring-blue-500 focus:outline-none dark:text-slate-300 dark:hover:bg-slate-800"
				>
					<svg
						id="theme-toggle-dark-icon"
						class="hidden h-5 w-5"
						fill="currentColor"
						viewBox="0 0 20 20"
						aria-hidden="true"
						><path
							d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"
						></path></svg
					>
					<svg
						id="theme-toggle-light-icon"
						class="hidden h-5 w-5"
						fill="currentColor"
						viewBox="0 0 24 24"
						aria-hidden="true"
						><path
							d="M12 2.25a.75.75 0 01.75.75v2.25a.75.75 0 01-1.5 0V3a.75.75 0 01.75-.75zM7.5 12a4.5 4.5 0 119 0 4.5 4.5 0 01-9 0zM18.894 6.166a.75.75 0 00-1.06-1.06l-1.591 1.591a.75.75 0 101.06 1.06l1.591-1.591zM21.75 12a.75.75 0 01-.75.75h-2.25a.75.75 0 010-1.5H21a.75.75 0 01.75.75zM17.834 18.894a.75.75 0 001.06-1.06l-1.591-1.591a.75.75 0 10-1.06 1.06l1.591 1.591zM12 18a.75.75 0 01.75.75V21a.75.75 0 01-1.5 0v-2.25A.75.75 0 0112 18zM7.758 17.303a.75.75 0 00-1.061-1.06l-1.591 1.591a.75.75 0 001.06 1.061l1.591-1.591zM6 12a.75.75 0 01-.75.75H3a.75.75 0 010-1.5h2.25A.75.75 0 016 12zM6.697 7.757a.75.75 0 001.06-1.06l-1.59-1.591a.75.75 0 00-1.061 1.06l1.59 1.591z"
						></path></svg
					>
				</button>
			</div>

			<!-- Mobile Menu Button -->
			<div class="flex items-center md:hidden">
				<button
					id="theme-toggle-mobile"
					type="button"
					aria-label="Toggle dark mode"
					class="mr-4 rounded-full p-2 text-slate-600 hover:bg-gray-100 dark:text-slate-300 dark:hover:bg-slate-800"
				>
					<svg
						class="theme-icon-dark hidden h-5 w-5"
						fill="currentColor"
						viewBox="0 0 20 20"
						aria-hidden="true"
						><path
							d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"
						></path></svg
					>
					<svg
						class="theme-icon-light hidden h-5 w-5"
						fill="currentColor"
						viewBox="0 0 24 24"
						aria-hidden="true"
						><path
							d="M12 2.25a.75.75 0 01.75.75v2.25a.75.75 0 01-1.5 0V3a.75.75 0 01.75-.75zM7.5 12a4.5 4.5 0 119 0 4.5 4.5 0 01-9 0zM18.894 6.166a.75.75 0 00-1.06-1.06l-1.591 1.591a.75.75 0 101.06 1.06l1.591-1.591zM21.75 12a.75.75 0 01-.75.75h-2.25a.75.75 0 010-1.5H21a.75.75 0 01.75.75zM17.834 18.894a.75.75 0 001.06-1.06l-1.591-1.591a.75.75 0 10-1.06 1.06l1.591 1.591zM12 18a.75.75 0 01.75.75V21a.75.75 0 01-1.5 0v-2.25A.75.75 0 0112 18zM7.758 17.303a.75.75 0 00-1.061-1.06l-1.591 1.591a.75.75 0 001.06 1.061l1.591-1.591zM6 12a.75.75 0 01-.75.75H3a.75.75 0 010-1.5h2.25A.75.75 0 016 12zM6.697 7.757a.75.75 0 001.06-1.06l-1.59-1.591a.75.75 0 00-1.061 1.06l1.59 1.591z"
						></path></svg
					>
				</button>
				<button
					id="mobile-menu-btn"
					type="button"
					aria-label="Open menu"
					aria-expanded="false"
					class="rounded-md p-2 text-slate-600 hover:bg-gray-100 focus:outline-none dark:text-slate-300 dark:hover:bg-slate-800"
				>
					<svg
						class="h-6 w-6"
						fill="none"
						viewBox="0 0 24 24"
						stroke="currentColor"
						aria-hidden="true"
						><path
							stroke-linecap="round"
							stroke-linejoin="round"
							stroke-width="2"
							d="M4 6h16M4 12h16M4 18h16"></path></svg
					>
				</button>
			</div>
		</div>
	</div>
	<!-- Mobile Menu -->
	<div
		id="mobile-menu"
		class="hidden border-b border-gray-200 bg-white md:hidden dark:border-gray-700 dark:bg-slate-900"
	>
		<div class="space-y-1 px-4 pt-2 pb-4">
			<a
				href="#portfolio"
				class="nav-link block rounded-md px-3 py-2 text-base font-medium text-slate-700 hover:bg-gray-50 hover:text-blue-600 dark:text-slate-200 dark:hover:bg-slate-800"
				data-target="portfolio">Portfolio</a
			>
			<a
				href="#resume"
				class="nav-link block rounded-md px-3 py-2 text-base font-medium text-slate-700 hover:bg-gray-50 hover:text-blue-600 dark:text-slate-200 dark:hover:bg-slate-800"
				data-target="resume">Resume</a
			>
			<a
				href="#contact"
				class="block rounded-md px-3 py-2 text-base font-medium text-slate-700 hover:bg-gray-50 hover:text-blue-600 dark:text-slate-200 dark:hover:bg-slate-800"
				>Contact</a
			>
		</div>
	</div>
</nav>

<script>
	// --- Dark Mode Logic ---
	const themeToggleBtn = document.getElementById("theme-toggle");
	const mobileToggleBtn = document.getElementById("theme-toggle-mobile");

	// Icons
	const themeToggleDarkIcon = document.getElementById("theme-toggle-dark-icon");
	const themeToggleLightIcon = document.getElementById(
		"theme-toggle-light-icon"
	);

	const mobileDarkIcon = document.querySelector(".theme-icon-dark");
	const mobileLightIcon = document.querySelector(".theme-icon-light");

	function updateIcons() {
		const isDark = document.documentElement.classList.contains("dark");

		// Desktop
		if (isDark) {
			themeToggleDarkIcon?.classList.add("hidden");
			themeToggleLightIcon?.classList.remove("hidden");
		} else {
			themeToggleDarkIcon?.classList.remove("hidden");
			themeToggleLightIcon?.classList.add("hidden");
		}

		// Mobile
		if (isDark) {
			mobileDarkIcon?.classList.add("hidden");
			mobileLightIcon?.classList.remove("hidden");
		} else {
			mobileDarkIcon?.classList.remove("hidden");
			mobileLightIcon?.classList.add("hidden");
		}
	}

	function toggleMode() {
		const html = document.documentElement;
		html.classList.toggle("dark");

		const isDark = html.classList.contains("dark");
		localStorage.setItem("theme", isDark ? "dark" : "light");

		updateIcons();

		// Dispatch custom event to notify other components of theme change
		window.dispatchEvent(
			new CustomEvent("theme-changed", {
				detail: { theme: isDark ? "dark" : "light" },
			})
		);
	}

	// Initialize
	updateIcons();

	// Event Listeners
	themeToggleBtn?.addEventListener("click", toggleMode);
	mobileToggleBtn?.addEventListener("click", toggleMode);

	// --- Mobile Menu Logic ---
	const btn = document.getElementById("mobile-menu-btn");
	const menu = document.getElementById("mobile-menu");

	btn?.addEventListener("click", (e) => {
		e.stopPropagation(); // Prevent immediate closing if we add document click listener
		menu?.classList.toggle("hidden");
	});

	// Close mobile menu when clicking outside
	document.addEventListener("click", (e) => {
		if (
			menu &&
			!menu.classList.contains("hidden") &&
			!btn?.contains(e.target as Node) &&
			!menu.contains(e.target as Node)
		) {
			menu.classList.add("hidden");
		}
	});

	// --- Navigation Logic ---
	const navLinks = document.querySelectorAll(".nav-link");
	navLinks.forEach((link) => {
		link.addEventListener("click", (e) => {
			const target = (e.currentTarget as HTMLElement).dataset.target;
			if (target) {
				window.dispatchEvent(
					new CustomEvent("view-change", { detail: target })
				);

				// Close mobile menu
				if (!menu?.classList.contains("hidden")) {
					menu?.classList.add("hidden");
				}
			}
		});
	});

	// --- Navbar Transparency Logic ---
	function updateNavbarTransparency(viewId: string) {
		const nav = document.querySelector("nav");
		if (!nav) return;

		if (viewId === "portfolio") {
			// Make solid for portfolio view to avoid color bleeding from white iframe
			nav.classList.remove(
				"bg-white/80",
				"dark:bg-slate-900/80",
				"backdrop-blur-md"
			);
			nav.classList.add("bg-white", "dark:bg-slate-900");
		} else {
			// Make transparent for other views
			nav.classList.add(
				"bg-white/80",
				"dark:bg-slate-900/80",
				"backdrop-blur-md"
			);
			nav.classList.remove("bg-white", "dark:bg-slate-900");
		}
	}

	window.addEventListener("view-change", (e: Event) => {
		const customEvent = e as CustomEvent;
		updateNavbarTransparency(customEvent.detail);
	});

	// Initial check
	const initialHash = window.location.hash.replace("#", "");
	if (initialHash === "portfolio") {
		updateNavbarTransparency("portfolio");
	}
</script>
